# =====================================================================================
#  iOS capabilities. Layered on top of global.properties when platform=ios.
# =====================================================================================
#  UNVERIFIED: wired but never executed. This project was built on Windows, and an
#  XCUITest session needs macOS with Xcode. These values are the shape an iOS run
#  takes, not values confirmed against a running simulator.
# =====================================================================================

device.name=iPhone 15
device.platform.version=17.0

# --- Application under test ---
app.bundle.id=org.wikimedia.wikipedia
app.path=

# --- Reset behaviour ---
app.no.reset=false
app.full.reset=false
